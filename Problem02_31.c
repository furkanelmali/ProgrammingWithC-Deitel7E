#include <stdio.h>
#include <math.h>

int main()
{
	int number = 0;
	printf("\nNumber \tSquare \tCube");

	printf("\n%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);

	number = number++;

	printf("%d \t%d \t%d \n", number, number * number, number * number * number);
	


}